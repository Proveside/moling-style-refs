# moling-style-refs

Public style-reference images for the **ai-image-studio** Cursor skill (Midjourney `--sref` source).

These images are hosted here **solely** to keep the skill fully decoupled from the
production website (molingnovel.com) — Midjourney `--sref` fetches only from
`raw.githubusercontent.com`, never from the production server. Zero impact on the live site.

## Raw base URL
`https://raw.githubusercontent.com/Proveside/moling-style-refs/main/<filename>`

## Pools (physically isolated, never mix in one `--sref`)
- `01-..17-*.png` — epic-fantasy-anime-vista (niji 7 line-art)
- `cpf-*.png` — cinematic-painterly-fantasy (V7 soft matte)
- `crf-*.png` — cinematic-realistic-fantasy (V7 realistic/oil)
- `agk-*.png` — anime-gacha-keyart-fantasy (V7 thick-paint gacha)
